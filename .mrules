# ZwerGe-UI · Repository Contract

> 本文件是所有 coding agent 必读的**最终规则文件**（会被无条件注入上下文，必须保持精简）。
> 全量变更历史与工程细节见 `.mrules.log`（3131 行，按 §九 索引检索，不再逐条通读）。
> 论文 revision 阶段的记忆入口（审稿意见、补实验清单、叙事原则）见 `README.md`。

论文：*Middle Layers Know Where to Click: Coordinate Serialization Bottlenecks in GUI Agents*（AAAI 2027，Submission2403）。
核心 claim：**Coordinate Serialization Bottleneck** —— GUI grounding 的空间定位信号在中间层已形成（spatial lens），
最后几层偏向把空间意图"序列化"为坐标 token（serialization lens），二者峰值层错位。
方法：frozen backbone + Stage1 独立 CrossAttn probe（~42M，0.5% of 8B）+ Stage2 跨层融合（<200K）+
training-free margin-gated ensemble（fusion 点 vs backbone native 坐标按 margin 门控）。
当前阶段：**AAAI rebuttal 准备**（评分 2/3/3.5，三位审稿人意见在 `docs/reviews/`，应对清单在 `README.md` §2）。

---

## 一、仓库目录结构（2026-09-22 重组后，对齐 a2ui 两级 git 组织）

```
code/                            # git 仓库根（github.com/HashTAG00002/zwergeUI）
├── .mrules                      # 本文件（契约+索引，精简）
├── .mrules.log                  # 全量变更日志（检索用，不再注入）
├── README.md                    # revision 阶段唯一记忆入口（先看这个）
├── docs/
│   ├── our_tex/                 # 论文源码（git subtree ↔ Overleaf 双向同步，见 §三）
│   │   ├── AnonymousSubmission2027.tex  # 主文（AAAI submission 版）
│   │   ├── CameraReady2027.tex          # camera-ready 版
│   │   ├── supplementary.tex + inject_labels.py + build.sh  # 附录独立编译（跨文档标签注入）
│   │   ├── secs/  figs/  tables/  prompt/  aaai2027.{bib,bst,sty}
│   │   └── scripts/ + .env.local        # 本地同步工具与凭据（gitignore，不进 git 不进 overleaf）
│   ├── reviews/                 # 三位审稿人意见原文（Reviewer-{eKpD,4KeN,4sok}）
│   ├── oracle/                  # 调研/handoff 文档（report、writing_suggestion、chatgpt-export 等）
│   ├── record/                  # 调试历史原始记录（debug/fusion）
│   └── references/              # 竞品论文原文（gitignore，共享盘持久）
├── zwerge/                      # 核心代码（python 包）
│   ├── src/zwerge_retrofit/     # 建模：modeling_base.py + modeling_{uitars,uitars1,guiowl,guiowl7b,uivenus,qwen35}.py
│   │                            #       + constants.py / dataset.py / trainer.py
│   ├── eval/                    # 评测：inference_base.py + 各模型 inference_*.py + eval_retrofit.py（统一入口）
│   │                            #       + p2p_sweep.py / submit_p2p_jobs.py / case_study_failures.py / vis_utils.py
│   ├── probe/                   # 论文 §3 分析实验：exp3_serialization_lens.py / exp4_counterfactual.py /
│   │                            #       analysis_{ood,bootstrap_ci,targetsize,fusion_ablation,norm_census,
│   │                            #       random_instruction,tuned_lens}.py + probe_utils.py + plot_probes.py
│   ├── experiments/*.yaml       # eval_daemon 实验配置（A3/A7/A8 各模型）
│   ├── scripts/                 # 训练 hope/shell（train_ablation_A0~A8）+ eval hope
│   ├── train_retrofit.py        # 训练主入口
│   ├── eval_daemon.py           # 异步评估守护进程（训练/评测解耦）
│   └── debug_contracts/         # Qwen3 兼容性审计文档（compat_report.md + 环境验证脚本）
└── scripts/sync_github.sh       # GitHub 推送（见 §三）
```

父目录（不进 git）：`zwerge/data/`（eval 数据、results 产出）、`zwerge/infer_jc/`（另一套独立推理代码，勿动）。
历史沿革：GUI-AIMA/GUI-Actor 子模块已移除；`docs/our_paper_tex` 已更名 `docs/our_tex`。

## 二、模型权重与 conda 环境速查

- 模型根目录（先 `ls` 确认子目录名，不要凭记忆猜路径）：
  GUI Agent 系列：`/mnt/dolphinfs/ssd_pool/docker/user/hadoop-mt-ocr/yangwenkui03/.hdd/models/huggingface.co/GUI_Agents/`
  Qwen 系列：`/mnt/dolphinfs/ssd_pool/docker/user/hadoop-mt-ocr/yangwenkui03/.hdd/models/huggingface.co/Qwen/`
- conda 环境：uitars/guiowl7b 训练=`gui_actor`；uitars1=`qwen2`；评测(Qwen2.5)=`qwen25`；
  guiowl/uivenus(Qwen3-VL)=`qwen3`（⚠️ 勿用 qwen3-verl，Conv3d 退化 bug；`conda run -n qwen3` 可能解析错，
  直接调 `/mnt/.../conda/envs/qwen3/bin/python`）；qwen35=`qwen35`。
- 论文 checkpoint：A8 checkpoint-2800（主结果）；ckpt 根 `/mnt/.../.hdd/ckpt/zwerge/`。

## 三、Git 两级同步（Overleaf + GitHub）

```bash
# 论文 → Overleaf（git subtree 压平单提交，永远 fast-forward，触发云端重编译）
bash docs/our_tex/scripts/sync_to_overleaf.sh push    # 本地 → overleaf（改前若网页端动过先 pull）
bash docs/our_tex/scripts/sync_to_overleaf.sh pull    # overleaf → 本地

# 仓库总代码 → GitHub
bash scripts/sync_github.sh                            # push 当前分支到 origin

# 本地编译论文
bash docs/our_tex/build.sh                             # 完整：主文+附录+跨文档标签注入
bash docs/our_tex/scripts/compile.sh [main]            # 快速：只编主文（pdflatex×3+bibtex）
```

- Overleaf 项目：https://www.overleaf.com/project/6a5edf2d15bb366a14bb9277（凭据在
  `docs/our_tex/.env.local`，gitignore，**绝不提交**；脚本会自动 source）。
- GitHub 凭据：`credential.helper=store` + `~/.git-credentials`（有效）。
  ⚠️ 不要用 `.git/config` 里曾内嵌的 PAT（已过期 401）；remote URL 保持无内嵌凭据形式。
- 代理（2026-09-22 修复）：死代理 `10.70.11.143:8412` 实际内嵌在**本仓库** `.git/config`（非全局，
  旧记录误写），config 优先级高于环境变量，是裸 git push Connection refused 的根因，已删除；
  现全局 `~/.gitconfig` 设 `http.proxy=http://10.70.16.106:3128`，裸 git push/pull/fetch 直接可用；
  两个 sync 脚本的 `-c` 覆盖保留为双保险（优先级最高，不冲突）。
- push 到 overleaf 只推**已提交**的 `docs/our_tex` 树；PDF/凭据/scripts 已被 gitignore 隔离。

## 四、工程约束（写代码必须遵守）

1. FA2 兼容：主训练路径必须走 flash_attention_2，禁止依赖 `output_attentions=True` 保存完整 attention matrix。
2. 显存：禁止多层同时保存完整 (B,L,H,S,S) attention weights；分析路径只对少数层做 sparse QK recompute。
3. 高风险 bug（扩展新模型/新实验几乎必复发，详细根因见 .mrules.log 与 README §5）：
   - `reinit_grounding_head`：Stage2/续训必须显式 `--reinit_grounding_head false`，否则静默擦除已训权重。
   - bf16 `from_pretrained` 新增 LayerNorm 权重是 NaN（未初始化内存）→ 必须调 `reinit_grounding_head()`。
   - 深层 hidden state L2 norm 爆炸（L26 可达 1.4e4）→ 任何新 probe 必须在 MLP/LN 前做 RMS pre-scale。
   - Qwen3-VL Conv3d 退化是 PyTorch 版本问题（2.8 已修/2.9.1 有 bug），**禁止重新引入 Conv3d→Linear
     monkey-patch**（bf16 下系统性数值漂移，已被证明是伪修复）。
   - DDP+GC：显式 `gradient_checkpointing_kwargs={"use_reentrant": False}` + `ddp_find_unused_parameters=True`。
   - 新模型接入先查 config.json `architectures` 对应 transformers 类（同系列模型类无继承关系）。
4. RL 范围：GRPO 只施加在 zoom controller，不对整 backbone 做在线 RL。

## 五、评测基准与输出

- P0：ScreenSpot-Pro（高分辨率小目标主战场）；P1：ScreenSpot-v2（已饱和）；P2：OSWorld-G；
  P3：MMBench-GUI L2 / UI-Vision。
- 4 个 backbone：GUI-Owl-7B / UI-TARS-1.5-7B（Qwen2.5-VL 28层）、GUI-Owl-1.5-8B / UI-Venus-1.5-8B（Qwen3-VL 36层）。
- 指标：主表统一 strict **overlap@1**（margin-gated ensemble，τ=0.20，24 cell 与 baseline 同次运行协议对称）。
- 评测数据：`/mnt/.../yangwenkui03/datasets/evaluation/`（各 bench eval.json 已标准化）；
  结果输出 `/mnt/.../yangwenkui03/zwerge/data/results/`，禁用 find 递归遍历，只用 ls/ll。
- 训练数据：grounding_200k.jsonl（AgentNet 42k + OS-Atlas 35k + GroundCUA 123k，已去重+排除测试集）。

## 六、论文叙事边界（写作红线，完整版见 README §3/§6 与 .mrules.log §六）

- 定位：**analysis-primary, method-secondary**；主打 "Coordinate Serialization Bottleneck"，
  **不要主打 "before decoding"**（避免与 Re-Prefill arXiv:2605.12549 混淆，Related Work 一段边界声明即可）。
- 禁用绝对化表达：不写 "improves across five benchmarks" 这类话；appendix 定性描述必须逐项对数据。
- 三维差异化坐标系：① uncertainty 来源（single-pass 内部状态 vs 多次采样/解码时）；
  ② layer-wise 建模（多层 hidden-state probe FA2 兼容 vs 全层 attention output_attentions）；
  ③ zoom 触发（learned controller vs 启发式/外部 RL）。
- naming 统一 `\textsc{ZwerGe-UI}`；正文 7 页限制（AAAI），编译全程 0 fatal / 0 undefined ref。

## 七、.mrules.log 检索索引（全量历史 3131 行，按需取阅，勿全文通读）

**§一~§六 原始项目规则**（L1-247）：代码库结构（含已移除的 GUI-AIMA/GUI-Actor 子模块）、方法三层结构、
竞品区分策略详表（Re-Prefill/AutoFocus/UI-Zoomer 等 7 方向）、SOTA 数字参考（2026-05 时点）。

**奠基期 2026-04-29~30**（L248-333）：layer_probe 启发式规则盘点（patch_scores_to_point 三层嵌套假设）、
与 GUI-AIMA/GUI-Actor 官方实现差异对照、Retrofit 训练框架搭建（zwerge/ 子模块创建）、vis 模式。

**NaN 修复与 Grounding Head 技术文档 2026-05-08~09**（L334-666）：深层 norm 爆炸根因链、
RMS pre-scale 方案、head 初始化策略与架构详述（LoRA adapter/shared MLP/FusionScorer）、
bf16 NaN 三层修复（reinit_grounding_head 的由来）、AFO MASTER_PORT 修复、WandB 指标。

**竞品与数据 2026-05-17~18**（L667-754）：竞品调研与叙事边界、leaderboard 数字、
5 个评测数据集标准化（eval.json 统一格式、数量口径）、eval 脚本端到端 dry-run。
**推理机制与 Token 设计**（L755-898）：<|ground|> anchor 选取 P1-P5、prefill-only 推理链路、
backbone 比特级冻结验证、新 token embedding mean init + gradient hook、agent 集成方案 A/B。

**解码与融合 2026-05-19~20**（L899-1164）：centroid/argmax/peak_shift/temperature 四策略
（结论：centroid 最优，argmax 反效果，hit<overlap 根因=28px 粗网格量化）、Gaussian GT 标签、
cos_meta fusion（α_l + cos(q_meta, q_l)）、ValEvalCallback（已被 eval_daemon 取代）。

**多模型与 Qwen3 兼容性 2026-05-21~22**（L1165-1544）：GUI-Owl/UI-Venus 适配、Conv3d 性能退化
（先 monkey-patch 后证明是伪修复并移除的完整教训）、DDP+GC 修复、eval/ 镜像重组、
A6 独立层监督 / A7 CrossAttn probe（全秩 W_q/W_k，0.53% 参数量）。

**zoom 与异步评估 2026-05-22~26**（L1545-2303）：zoom_backbone 两阶段精化（坐标映射 bug：
UI-TARS 输出绝对像素非 /1000）、eval_daemon 异步评估架构（is_permanent_ckpt 标记 + WandB 回传）、
A7 四实验启动、qwen3 环境验证、Qwen3.5(qwen35)/UI-TARS-7B-SFT(uitars1) 适配。

**A8 双阶段训练 2026-05-25**（L2304-2449）：ContextLoRACosMetaFusion 结构、active-subset 设计
（14 probe 中激活 10 层）、Stage2 梯度路径全解析、6 条易错注意事项（probe_layers 必须与 A7 一致等）。

**AAAI 冲刺 2026-07-28~29**（L2450-3131）：
- P2P training-free 解码器（L2450, L2581）：Level1 区域重排/Level2 跨层共识/Level3 局部高斯反演、
  公平协议修复（overlap@3→strict overlap@1）、Qwen3 负优化重新框定（bottleneck severity：
  Qwen3 native 已超 best probe，retrofit headroom 为负=预期 null 结果）、native gate 回收负优化。
- 分析性 probe 实验（L2627-2899, L2949）：交接文档、P0-4 OOD 泛化（L* 跨 bench 漂移 ≤3 层）、
  P1-2 bootstrap CI（L† CI≤1 层）、P1-3 target-size 受控回归（绝对 final hit@1 小目标 1.7× 差）、
  P1-4 fusion 消融（learned 比 final-only 高 7-14pp，跟踪 oracle 2pp 内）、P2-1 norm census
  （Qwen2.5 3.4×/Qwen3 7.1-7.8× 深度增长，Registers 类比）、P0-2a 非坐标 token 对照、
  P0-2b tuned-lens（plateau 位置抗校准）、P0-3 random-instruction（hit@1 40%→1.5% 坍缩）、
  全系列数字核查（修复 5 处论文与数据不一致）。
- margin-gated ensemble 主表（L2768-2948, L3013）：mass gate 失败→margin gate（peak−2nd peak>0.25）
  7/8 beat base 0 below→主表 τ=0.20 全部 24 cell 同次运行严格 overlap@1（宏观均值 +0.46pp），
  全文 gate 叙事重写。
- 论文打磨（L2840, L3076）：AAAI 合规修复（禁 \clearpage/colortbl/tcolorbox label）、
  结构重组（2_probe.tex 前置，7 页达标）、去破折号、训练/测试数据入 appendix、
  P2P decoder 细节入 appendix、fig3 legend 修复。

**pack.sh 打包脚本迁移 2026-09-22**（log 末尾条目）：a2ui/pack.sh 适配到仓库根，
git archive HEAD 只打 tracked 文件 + unzip -Z1 计数校验（默认输出 ZwerGeUI_main_<SHA>.zip），
实测 184/184 一致、敏感 gitignore 项（.env.local/references/wandb）零泄漏。

**维护规则**：每轮对话结束前将核心变动以同样格式追加到 .mrules.log 末尾（一条 = 日期+主题+可验证细节），
本文件的 §七 索引同步加一行；.mrules 正文只在契约级事实变化时修改。

---

## 八、变更日志（本文件自身）

- [2026-09-22] 仓库重组（对齐 a2ui）：`.mrules` 拆分为契约+索引（本文件）与全量历史（`.mrules.log`，
  原 3132 行全文保留）；`docs/our_paper_tex` 更名 `docs/our_tex` 并打通 Overleaf 双向同步
  （project 6a5edf2d15bb366a14bb9277，首次 push 已验证 tree 一致）；GitHub 同步链路修复
  （内嵌死 PAT → credential store）；清理 debug/临时/叶子文件约 9900 行 + wandb 271M；
  详见 .mrules.log 末尾 [2026-09-22] 条目。
- [2026-09-22] git 代理修复：删本仓库 `.git/config` 内嵌死代理 `10.70.11.143:8412`（config 压制
  环境变量致裸 git push refused；旧记录误写为"全局"），全局 `~/.gitconfig` 设活代理
  `http://10.70.16.106:3128`；详见 .mrules.log 末尾 [2026-09-22] 条目。
