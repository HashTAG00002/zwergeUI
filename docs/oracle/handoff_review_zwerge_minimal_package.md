# 交接任务：独立审查 ZwerGe-UI 补充材料最小代码包

## 任务背景

本仓库正在准备一篇论文（论文源码位于 `docs/our_paper_tex/`，主文件
`AnonymousSubmission2027.tex`，各小节在 `docs/our_paper_tex/secs/*.tex`）的
AAAI 投稿补充材料。有人（不是你）已经从项目的完整训练/推理代码库
（主要是 `zwerge/src/zwerge_retrofit/` 和 `zwerge/eval/` 下的文件）中提炼出
了一个"最小化、自包含"的代码包，声称可以单独说明论文方法部分的核心算法，
不依赖任何真实模型权重、不依赖任何私有数据。

这个包已经打包为一个 zip 文件：

```
docs/our_paper_tex/supplementary_code/zwerge_minimal.zip
```

解压后应包含且仅包含以下文件（如果实际内容与此不符，这本身就是一个问题，
请如实报告）：

```
zwerge_minimal/
  modeling.py
  inference.py
  ensemble.py
  probe_lens.py
  README.md
  requirements.txt
  example_run.sh
```

## 你的任务

**你需要完全独立地判断这个代码包是否合格，不能预设它是对的，也不能参考
任何关于"之前有人已经验证过"的说法（即使你在其他上下文中看到类似说法，
也请自行重新验证，不要采信）。** 你没有被告知这个包是谁做的、是否已经
测试过、测试结果如何——请把它当作一份需要从零开始质检的交付物。

请按以下几个维度独立完成检查，并分别给出你自己的结论和证据（不要只给
"通过/不通过"的结论，要给出你验证时具体做了什么、看到了什么输出）：

### 维度 0：匿名性合规审查（AAAI 双盲评审要求，优先级最高，必须最先做）

这份代码是要放进 AAAI 投稿的**补充材料**里的，AAAI（以及绝大多数顶会）
对补充材料有明确的双盲匿名性要求：补充材料中**不得包含任何可能直接或
间接暴露作者身份、所属机构、内部基础设施的信息**。这是一票否决项——
哪怕代码逻辑和论文完全一致、跑起来也没问题，只要存在任何去匿名化风险，
这个包就不合格，必须在报告里用最高优先级单独列出。

请对 zip 包解压后的**全部文件内容**（包括代码注释、docstring、字符串
字面量、文件名、`README.md`、`example_run.sh`）做系统性排查，重点检查
但不限于以下几类信息（请自己设计检索关键词和检索方法，不要只用某一个
固定的关键词列表，因为真实的泄露点可能以你没预设到的形式出现）：

1. **真实姓名/用户名/邮箱**：包括作者姓名全称、姓名拼音缩写、任何形式
   的个人邮箱地址（尤其是公司域名邮箱）、任何 IM/聊天工具账号。
2. **机构/公司信息**：公司名称（含中文名、英文名、内部代号）、内部
   团队名、内部项目代号、任何能推断出"这是哪家公司/实验室做的"的
   措辞（哪怕不是直接点名，比如某个非常独特的内部系统名称也可能被
   业内人士识别出所属公司）。
3. **内部基础设施痕迹**：内部文件系统路径（例如形如
   `/mnt/xxx/docker/user/<用户名>/...` 这种明显是某个人在某台内部
   机器上的家目录/工作目录的路径写死在代码或注释里）、内部集群/任务
   调度系统名称、内部 GPU 队列名、内部 conda/虚拟环境命名习惯、任何
   `http://内网IP:端口` 形式的地址、任何指向内部 wiki/知识库/工单
   系统的链接或页面 ID。
4. **外部服务账号信息**：如果代码里出现了任何第三方 SaaS 服务
   （实验跟踪平台、云存储、代码托管平台等）的项目名/实体名/组织名，
   而这个名字本身包含了作者姓名、拼音、或机构信息的，也算泄露
   （即使服务本身是公开可访问的第三方工具，只要"项目归属名"暴露了
   身份也不行）。同样要检查是否有任何真实可用的 API key / token
   （这既是隐私也是安全问题）。
5. **GitHub / 代码托管平台链接**：任何指向真实、非匿名化的个人或组织
   代码仓库主页的链接（`github.com/<真实用户名或组织名>/...`）。如果
   论文需要引用自己之前的仓库，标准做法是用占位符（如
   `[anonymized]`、`https://anonymous.4open.science/...`）而不是真实
   链接，请检查代码里有没有真实链接漏网。
6. **文件系统/压缩包元数据**：zip 内每个文件的元数据（用
   `unzip -l -v` 或 `zipinfo` 查看，也可以用 `exiftool`/`stat` 查看
   解压后文件的属性）里，是否残留可关联到真实系统用户名的信息
   （比如 Unix 用户名、用户 UID、机器 hostname 片段）。
7. **git/版本控制残留**：确认包内没有 `.git` 目录、没有任何 git
   commit 历史文件、没有能追溯到真实 commit author 的信息。
8. **间接指纹信息**：即使没有直接的姓名/邮箱，也请留意是否存在一些
   "组合起来能缩小到极少数人/团队"的间接线索，例如：极其特殊的内部
   代号式变量命名、只有内部人士才会用的缩写、注释里提到的具体日期+
   具体内部里程碑事件的组合、代码风格上明显复制自某个可公开搜索到的
   个人代码仓库特征等。这一类需要你运用判断力，不是简单的字符串匹配
   能覆盖的，请说明你是如何排查的。

**检查方法建议**（你可以用你认为更好的方法，这里只是提示，不是限制）：
对解压后的目录做递归全文检索，同时用大小写不敏感的方式搜索若干类
关键词（用户名模式、常见邮箱域名后缀、内部路径特征词、`http://`/`https://`
链接、常见内部工具类名词等），并且**不要只依赖关键词命中**——请至少
完整通读一遍每个 `.py` 文件的文件头 docstring、每个函数/类的 docstring，
以及 `README.md` 全文和 `example_run.sh` 全文，用人工判断的方式再过一遍，
因为很多去匿名化信息不会被简单关键词命中（比如姓名的不同拼写变体、
被拆分在多行注释里的路径片段等）。

请把维度 0 的检查结果作为报告的**第一部分**呈现，并且如果发现任何疑似
问题，无论你觉得风险高低，都必须逐条列出「发现位置（文件名+行号）」
「具体内容（可以打码敏感部分但要说明发现了什么类型的信息）」「你认为
的风险等级」。如果确认未发现任何问题，也要说明你具体检查了哪些方面、
用了哪些方法，不能只写一句"未发现问题"。

### 维度 1：包的完整性与可运行性

1. 解压 zip，确认文件清单与上方一致，检查是否有多余文件（如 `__pycache__`、
   临时脚本、`.pyc`、编辑器备份文件等不应该出现在交付物中的东西）。
2. 对 `modeling.py`、`inference.py`、`ensemble.py`、`probe_lens.py` 分别执行
   `python -m py_compile <file>`，确认语法层面全部通过。
3. 检查这 4 个核心 `.py` 文件互相之间的 `import` 关系是否能在**没有安装
   任何真实 backbone 权重、没有 GPU、只有 CPU + torch + transformers**
   的环境下正常 `import`（哪怕只是 `import modeling`、`import inference`、
   `import ensemble`、`import probe_lens` 这种最基本的模块级导入，不牵扯
   真实推理）。如果导入失败，记录清楚是哪一行、缺什么依赖、是否是设计上
   的"lazy import"（即只有真正调用某个函数时才需要额外依赖，模块级
   import 本身不应该报错）。
4. 独立编写（不要复用你可能见过的任何现成测试脚本）一组你认为合理的
   **纯 CPU、纯合成张量**的功能性冒烟测试，去实际调用这 4 个文件里核心的
   类和函数（不要只测 import 能不能成功，要真正调用一遍，看输出是否
   数值上合理，比如概率分布是否非负、是否归一化到 1、形状是否符合预期、
   反向传播是否能跑通、梯度是否非零、坐标解码结果是否落在图像范围内等）。
   完成后请把你自己写的测试脚本内容和运行结果都写进你的报告里。
5. 检查 `README.md` 里给出的运行说明（如果有 `example_run.sh` 或代码片段）
   是否真的能对应到包内实际存在的函数签名，是否存在"文档说的接口和代码
   实际接口对不上"的情况。
6. 检查 `requirements.txt` 里列出的依赖版本号/包名是否与代码里实际用到的
   import 一致（比如代码里 `from transformers.models.qwen2_5_vl...` 但
   requirements 里完全没提 transformers，就是不一致）。

### 维度 2：与论文正文的一致性（这是最重要的部分）

论文正文位于 `docs/our_paper_tex/secs/`，具体建议你重点读这几个文件：

- `2_probe.tex`（对应 label `sec:probe` / `sec:method:stage1`，含公式
  `eq:probe`、`eq:gaussian`）：描述了逐层 grounding probe（Stage 1）的
  数学形式。
- `4_method.tex`（对应 label `sec:method`，含 `sec:method:stage2`、
  公式 `eq:fusion`、`eq:loss_s2`，以及 `sec:method:ensemble`）：描述了
  跨层融合（Stage 2）和"Training-Free Gated Decoding"（部署时的
  margin-gated ensemble 解码器）。
- `3_analysis.tex`：描述了论文的分析性结论（spatial lens / serialization
  lens、layer-wise 现象等），这部分对应代码包里的 `probe_lens.py`。
- `a_appendix.tex`：可能包含 P2P 解码器（Level 1/2/3）更详细的算法描述。
- 摘要（`AnonymousSubmission2027.tex` 里的 abstract）和 `1_intro.tex`、
  `6_conclusion.tex`：确认代码包体现的方法是否与论文对外宣称的核心贡献
  point 一致。

请你自己通读论文中这几处的公式和文字描述，然后逐条核对代码包里对应的
实现，重点确认（但不限于）以下几点，每一点都要给出你自己读到的论文原文
片段 + 代码里对应的行号/函数名，不能只写"一致"或"不一致"这种没有证据
的判断：

1. **Eq. 1（per-layer probe）**：论文里 probe 的具体计算方式（用什么做
   query/key，是否多头，是否有可学习的 head 组合权重，是否有 LoRA
   还是全秩投影，参数量级是多少）是否和 `modeling.py` 里实现 probe 的
   那个类完全对应。特别注意论文里如果提到了"某种数值稳定性处理"（比如
   RMS 预缩放、LayerNorm 顺序等），代码里是否真的实现了，实现的位置和
   论文描述的动机是否吻合。
2. **Eq. 2（监督标签）**：论文里 Stage 1 的监督目标是什么形式（binary
   mask？各向异性高斯？球形高斯？），代码里生成 label 的函数是否严格
   对应论文公式（包括 sigma 与 bbox 宽高的关系、归一化方式、退化情形
   的处理）。
3. **Eq. 3（跨层融合）+ Eq. 4（Stage 2 loss）**：论文里 Stage 2 的融合
   机制具体是怎么组合多层 probe 输出的（是简单平均？还是有 LoRA + 
   context + cosine 相似度打分？温度参数是否可学习？），以及最终的
   训练目标（loss）是怎么组合 fusion loss 和 per-layer loss 的（是否
   有一个 lambda 系数，数值是多少）。代码里对应的类和 loss 计算是否
   严格匹配论文公式，包括系数、符号、reduction 方式。
4. **"Training-Free Gated Decoding" / 部署解码器**：论文里如果描述了
   一个"用某种置信度门控在融合预测点和 backbone 原生自回归坐标之间
   做选择"的机制，请找到论文里给出的**具体门控公式**（是用后验概率
   的 top1 减 top2 的 margin？还是某个 patch 的概率质量 mass？阈值
   tau 具体是多少，论文正文和附录里如果给出了不同的默认值，是否有
   说明主实验用的是哪个），并去 `ensemble.py` 里找到实现这个门控的
   函数，逐字对比公式和代码逻辑是否一致。**这一点请格外仔细**，因为
   如果代码实现的门控公式、默认阈值、或者"用什么去代表 margin/mass"
   与论文文字描述不一致，这是一个严重问题，需要明确指出具体哪里不
   一致（论文说的是 A，代码做的是 B）。
5. **P2P / 训练-free 后处理解码器的三个层级**（如果论文里提到了类似
   "区域重排序 / 跨层一致性 / 局部子像素修正"这样分三步的解码流程）：
   确认这三步在代码里是否都有实现，是否都不含可学习参数（论文如果强调
   这是"training-free"，代码里对应的函数就不应该有 `nn.Parameter` 或
   需要训练的权重）。
6. **锚点 token / zero label leakage 的说法**（如果论文里提到了类似
   "在生成任何坐标 token 之前就读取某个锚点 token 的隐藏状态，从而不会
   泄露标签"的设计）：确认代码里定位锚点位置的逻辑，是否真的保证了
   读取点在任何坐标信息生成之前，还是存在你认为可能有标签泄露风险的
   fallback 分支（如果代码里自己用警告或注释承认了某个 fallback 分支
   有风险，也请如实转述，但请你自己判断这个说法是否合理）。

对每一条，请你**自己去读论文原文的具体句子/公式**，摘录出来，再摘录
代码里对应的实现片段，两相对照后给出你的判断（一致/不一致/部分一致/
论文表述模糊无法判断），并说明理由。不要只依赖本文档里的转述去下结论，
本文档只是给你指路，不代表结论。

### 维度 3：与完整代码库的一致性（可选，用于交叉验证）

这个最小包是从项目里更完整的训练/推理代码中提炼出来的。如果你想进一步
交叉验证提炼过程有没有走样（比如简化时是否不小心改变了算法语义，而不只
是删掉了工程细节），可以去看这些原始文件作为参照（注意：这些文件是完整
训练框架的一部分，包含大量与本次审查无关的工程代码，你只需要找其中在
語义上对应最小包里同名/同功能的函数/类即可，不需要通读）：

- `zwerge/src/zwerge_retrofit/modeling_base.py`（对应最小包 `modeling.py`
  里的核心 probe / fusion / head 类，原始版本可能有更多 backbone 分支）
- `zwerge/eval/inference_base.py`（对应最小包 `inference.py`）
- 完整仓库里如果存在专门实现"P2P 解码 / 置信度门控 ensemble"逻辑的
  eval 脚本（自行搜索，可能命名包含 `p2p`、`ensemble`、`margin`、`gate`
  等关键词），对应最小包的 `ensemble.py`
- 完整仓库里如果存在专门做"逐层 spatial/serialization 分析"的脚本
  （自行搜索 `zwerge/probe/` 目录下以及 `GUI-AIMA/eval/layer_probe.py`
  等），对应最小包的 `probe_lens.py`

如果最小包里的某个函数在语义上明显简化/偏离了完整代码库里对应实现的
核心逻辑（不只是删掉了多 backbone 分支这种无害简化，而是数学公式本身
变了），请指出具体差异。如果只是工程上的裁剪（比如去掉了对其他 backbone
家族的支持、去掉了 wandb 日志、去掉了分布式训练相关代码）而核心数学逻辑
未变，则不算问题。

## 输出格式要求

请给出一份结构化报告，**按以下顺序**呈现，其中第 1 部分（匿名性）必须
放在报告最前面：

1. **匿名性合规审查结果（维度 0）**：逐条列出你检查过的 8 类风险点，
   每类给出你的检查方法 + 发现（如果有）+ 风险等级。最后给出一个明确
   的「匿名性：合规 / 不合规」结论，如果不合规必须精确到文件名+行号，
   不能笼统带过。
2. **可运行性检查结果**：逐项列出维度 1 的 6 个子项，每项给出 PASS/FAIL/
   PARTIAL 及具体证据（命令输出、报错信息等）。
3. **你自己编写并运行的功能性测试代码**（完整贴出）+ 运行输出。
4. **论文一致性逐条核对表**：至少覆盖维度 2 的 6 个点，每条给出「论文
   原文摘录（含文件名+大致行号或 label）」「代码实现摘录（含文件名+
   函数名/行号）」「你的判断（一致/不一致/部分一致/无法判断）」「理由」。
5. **（可选）与完整代码库的交叉核验结果**。
6. **总体结论**：这个最小代码包是否合格地、准确地代表了论文所声称的
   方法，并且是否满足投稿匿名性要求？如果你认为存在任何不一致、夸大、
   遗漏、或实现与描述不符的地方，请明确列出，哪怕只是很小的措辞级别的
   出入（比如论文说"默认 tau=0.25"而代码/文档里主结果用的是另一个数值，
   这种情况也要指出来，不要因为"结果本身可能没错"就忽略表述层面的
   不一致）。**如果维度 0（匿名性）发现任何未消除的风险，无论其他维度
   表现多好，总体结论都必须是"不合格，需先修复匿名性问题再考虑其他"**——
   匿名性问题在这份报告里拥有一票否决权，不能被"代码质量高/与论文一致"
   这类正面结论所抵消或稀释。

## 重要提醒

- **匿名性审查具有最高优先级和一票否决权**，请第一个做、认真做，不要
  把它当作和其他检查项同等权重的一项例行公事。这不是"代码写得好不好"
  的问题，而是"这份材料能不能提交"的问题。
- 请完全独立判断，不要假设这个包已经被验证过、已经没问题。你的价值就在
  于提供一次全新的、不受任何先验结论影响的审查。
- 如果你发现任何问题（哪怕是很小的不一致，或者一个看起来"应该问题不大"
  的疑似身份线索），请如实、具体地报告，不要为了让报告"看起来完整"或
  "看起来令人满意"而弱化问题描述、降低风险等级、或自行判断"这个应该
  没关系就不用写了"。宁可多报告、多标记为"疑似"，也不要漏报。
- 如果某一点因为论文写得比较模糊、或者你找不到对应描述而无法判断，请
  明确说"无法判断，原因是……"，不要强行给出一致/不一致的结论。
- 你不需要，也不应该去阅读或参考这个代码包是如何被创建出来的任何历史
  记录、commit message、聊天记录等——只依据代码包本身、论文正文本身、
  以及（可选）完整代码库本身这三者进行独立比对。
